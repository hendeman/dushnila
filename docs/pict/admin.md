# `skinali/pict/admin.py`

## Назначение

Настраивает Django Admin для всех моделей каталога и регистрирует их на сайте admin.

## `PictAdmin`

### Список записей

- 20 записей на страницу.
- Колонки: номер, миниатюра, категории, цвета.
- Ссылка открывается по полю `name`.
- Поиск: точное совпадение `name`, а также `tags__tag`. Точный поиск по номеру используется в autocomplete готовых работ.
- Фильтры: время добавления, категория, цвет.
- Сортировка: `-id`.

### Форма записи

- Поля: `name`, `alt`, `photo`, увеличенное превью, `time_update`, `color`, `cat`, `tags`. При редактировании изображения со связанными готовыми работами перед датой появляется дополнительное поле «Готовые работы» с их кликабельными миниатюрами шириной 160 px; для нового изображения и изображения без работ поле скрыто.
- Только для чтения: `time_update`, увеличенное превью и галерея готовых работ.
- Для `color`, `cat`, `tags` применяется горизонтальный виджет выбора.
- Используется [PictAdminForm](forms.md).

### Вспомогательные методы

- `get_html_photo(object)` возвращает кликабельное изображение шириной 150 px.
- `get_html_photo_fields(object)` возвращает кликабельное изображение шириной 400 px.
- `get_list_category(object)` соединяет названия категорий через запятую.
- `get_list_color(object)` соединяет названия цветов через запятую.
- `get_finished_works(object)` показывает фотографии всех записей `FinishedWork`, связанных с текущим номером изображения.

Для HTML-превью применяются `format_html` и `format_html_join`. URL берется из соответствующего `ImageField`.

## `FinishedWorkAdmin`

- Список содержит имя, миниатюру фотографии, номер связанного изображения, его категории и дату добавления; новые работы находятся сверху.
- Форма содержит имя, описание, одну фотографию, увеличенное превью, необязательное поле «Номер изображения» и дату создания.
- `catalog_image` использует `autocomplete_fields`: администратор вводит номер, а Django ищет точное совпадение по `Pict.name`.
- Если изображение каталога не выбрано, номер и категории в списке показываются как `—`.
- Превью создаются через `format_html`: 80 px в списке и 200 px в форме. Read-only превью в форме подписано «Миниатюра», чтобы отличаться от поля загрузки «Фото».
- `get_queryset()` заранее загружает связанный `Pict` и его категории через `select_related()` и `prefetch_related()`.

## `ContactRequestAdmin`

- Список показывает время создания, тип заявки, имя, телефон, вопрос и текущий статус Telegram; новые заявки находятся сверху.
- Доступны фильтры по типу и времени создания, а также поиск по имени, телефону и тексту вопроса.
- Время создания доступно только для чтения.
- В форме заявки встроена read-only таблица доставок со статусом, попытками, временем следующей попытки, подтвержденным `message_id` и последней ошибкой.
- Action «Поставить выбранные заявки в очередь Telegram» создает отсутствующую доставку для старых заявок; уже имеющиеся доставки не дублируются.
- На странице выводится по 50 заявок; модель не использует запросы к связанным таблицам.

## `ContactRequestDeliveryAdmin`

- Служебный список показывает заявку, канал, статус, количество попыток и даты следующей/успешной отправки.
- Данные доступны только для чтения и не создаются вручную через admin.
- Поиск выполняется по имени и телефону исходной заявки; доступны фильтры канала, статуса и даты создания.
- Admin action «Повторить выбранные неотправленные доставки» возвращает записи `retry`/`failed` в очередь и обнуляет счетчик попыток. Уже отправленные и находящиеся в обработке записи действие не затрагивает.

## Просмотр изображений в admin

`PictAdmin` и `FinishedWorkAdmin` используют общий `AdminImagePreviewMixin`. Он подключает `static/skinali/js/admin-image-preview.js` и `static/skinali/css/admin-image-preview.css`.

Все миниатюры каталога, готовых работ и блока связанных работ размечены атрибутом `data-image-preview`. Клик не переходит по прямому media-URL, а открывает исходный файл в нативном модальном `<dialog>`. Изображение меньше viewport сохраняет натуральный размер; большое пропорционально вписывается в доступную область. Окно закрывается крестиком, клавишей `Esc` или кликом по затемненному окружению. При отключенном JavaScript `href` остается запасной прямой ссылкой.

## Остальные admin-классы

- `CategoryAdmin`: показывает `cat`, `slug`; поиск по `cat`; slug автозаполняется из названия.
- `TagPictAdmin`: показывает `tag`, `slug`; поиск по `tag`; slug автозаполняется из тега.
- `ColorAdmin`: показывает `color`, `slug_color`; сортирует по названию цвета. Автозаполнение slug закомментировано.

## Регистрация

Зарегистрированы пары `Pict/PictAdmin`, `FinishedWork/FinishedWorkAdmin`, `ContactRequest/ContactRequestAdmin`, `ContactRequestDelivery/ContactRequestDeliveryAdmin`, `Category/CategoryAdmin`, `Color/ColorAdmin`, `TagPict/TagPictAdmin`.

## Зависимости

- [models.py](models.md).
- [forms.py](forms.md).
- Заголовки всего admin задаются в [корневом urls.py](../project/urls.md).

## Особенности и риски

- `PictAdmin.get_queryset()` заранее загружает категории, цвета и готовые работы; `FinishedWorkAdmin` заранее загружает связанное изображение и его категории.
- Аргумент методов назван `object`, перекрывая встроенный тип Python, но на работу это не влияет.

## Когда читать исходник

При изменении интерфейса admin, полей формы, поиска, фильтров, превью или производительности списка.
