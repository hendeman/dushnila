# Миграции приложений проекта

## Назначение

Сводка Python-файлов в `skinali/pict/migrations/` и `skinali/sitecontent/migrations/`. Миграции `pict` образуют линейную цепочку от `0001` до `0026`; `0025` также зависит от модели пользователя. У отдельного приложения управляемого содержимого своя начальная миграция. Оба `__init__.py` пусты и только обозначают пакеты.

Текущую итоговую схему удобнее смотреть в [models.md](../pict/models.md). Этот документ нужен для истории изменения БД и выбора миграции при диагностике.

## История

| Миграция | Изменение |
|---|---|
| `0001_initial.py` | Созданы `Category` и `Pict`; у `Pict` были строковые `name`, `title`, изображение, дата и M2M-категории. |
| `0002_alter_category_options_alter_pict_options_and_more.py` | Добавлены русские verbose names моделям и полям. |
| `0003_alter_pict_options_category_slug.py` | Добавлена сортировка `Pict` по `id`; в `Category` добавлен slug с временным default. |
| `0004_alter_category_slug.py` | Slug категории сделан уникальным. |
| `0005_color.py` | Создана модель `Color` с названием и уникальным slug. |
| `0006_pict_color.py` | В `Pict` добавлен nullable ForeignKey на `Color` с `CASCADE`. |
| `0007_rename_slug_color_slug_color.py` | Поле `Color.slug` переименовано в `slug_color`. |
| `0008_pict_alt_alter_pict_color.py` | Добавлено nullable-описание `Pict.alt`; цвет получил verbose name. |
| `0009_alter_pict_color.py` | Удаление цвета стало устанавливать ForeignKey в `NULL` (`SET_NULL`). |
| `0010_tagpict_pict_tags.py` | Создан `TagPict`; в `Pict` добавлена необязательная M2M-связь `tags`. |
| `0011_alter_tagpict_options_alter_tagpict_slug.py` | Добавлены verbose names тегов; slug преобразован в `SlugField`. |
| `0012_alter_pict_alt.py` | `alt` стал `blank=True` с временным default пустой строки. |
| `0013_alter_pict_name.py` | Строковый `name` стал уникальным и индексированным. |
| `0014_remove_pict_title.py` | Удалено поле `Pict.title`. |
| `0015_alter_pict_options_alter_pict_alt.py` | Сортировка изменена на `-id`; временный default у `alt` удален. |
| `0016_alter_pict_tags.py` | Для тегов добавлен `related_name='tags'`. |
| `0017_alter_category_slug_alter_color_slug_color_and_more.py` | Slug-поля получили текущие verbose names; `Pict.name` преобразован в integer; цвет заменен с ForeignKey на M2M; связи получили verbose names. |
| `0018_finishedwork.py` | Создана модель `FinishedWork` с именем, описанием, фотографией, датой создания и необязательной связью `ForeignKey` на `Pict` с `SET_NULL`. |
| `0019_contactrequest.py` | Создана независимая модель `ContactRequest` для валидных заявок: тип формы, имя, телефон, необязательный вопрос и время создания. |
| `0020_contactrequestdelivery_and_more.py` | Создана очередь `ContactRequestDelivery` со статусами, попытками, датами обработки, внешним ID, ошибкой, уникальностью заявки/канала и индексом готовых доставок. |
| `0021_contactrequest_email_message_fields.py` | Добавлен тип заявки `email_message`, настоящий email и комментарий; телефон модели стал необязательным для заявок без номера. |
| `0022_contactrequest_image_purchase.py` | Добавлен тип заявки `image_purchase`, необязательная связь заявки с `Pict` через `SET_NULL` и сохраняемый снимок номера изображения. |
| `0023_finishedwork_is_published_pict_is_published.py` | В `Pict` и `FinishedWork` добавлен включенный по умолчанию булев флаг `is_published` («Опубликовано»); существующие записи при применении миграции получают значение `True`. |
| `0024_contactrequest_viewed_at.py` | В `ContactRequest` добавлено необязательное не редактируемое формой поле `viewed_at`; `NULL` означает, что заявка ещё не открывалась в Django Admin. |
| `0025_integrations.py` | Созданы `Integration` и `IntegrationRevision` для управляемых HTML/JS-вставок и истории их снимков. |
| `0026_tag_search_terms.py` | В `TagPict` добавлено уникальное `normalized_tag`; существующие значения нормализованы data-миграцией; создана модель поисковых синонимов `TagAlias`. |
| `0027_taxonomy_seo_content.py` | В `Category` и `TagPict` добавлены необязательные H1, SEO-title, meta description и вводный текст публичной страницы. |

## Итог после `0027`

- `Category`, `Color`, `TagPict` — отдельные справочники с уникальными slug; `Category` и `TagPict` дополнительно хранят необязательные управляемые `seo_h1`, `seo_title`, `seo_description`, `intro_text`, а `TagPict.normalized_tag` — уникальное нормализованное поисковое значение.
- `TagAlias` каскадно принадлежит одному `TagPict` и хранит исходный синоним вместе с уникальным `normalized_alias`.
- `Pict.name` — уникальное индексированное целое число.
- Категории, цвета и теги связаны с изображениями many-to-many.
- `Pict.title` отсутствует.
- Изображения сортируются по убыванию `id`.
- `Pict.is_published` по умолчанию равен `True`; публичные выборки каталога используют только опубликованные записи.
- `FinishedWork` хранит одну фотографию готовой работы и при необходимости ссылается на одно изображение каталога; обратная связь — `Pict.finished_works`.
- Удаление связанного `Pict` не удаляет готовую работу, а обнуляет `catalog_image`.
- Готовые работы сортируются по убыванию времени создания и ID; независимый `FinishedWork.is_published` по умолчанию равен `True` и управляет публичным показом.
- `ContactRequest` хранит валидные заявки четырёх типов: телефонные формы используют телефон и вопрос, обычная email-форма — настоящий email и комментарий, а заявка покупки дополнительно хранит проверенную связь с `Pict` и снимок номера изображения. После удаления `Pict` снимок номера сохраняется. `viewed_at` фиксирует первый просмотр в admin; новые и исторические непросмотренные заявки имеют `NULL`. Записи сортируются от новых к старым.
- `ContactRequestDelivery` независимо отслеживает Telegram/email-каналы и каскадно принадлежит одной заявке; сейчас новые формы создают Telegram-доставку `pending`.

## Правила для ИИ-агента

- Не редактировать примененные исторические миграции для обычного изменения схемы; менять модель и создавать новую миграцию.
- Перед изменением схемы проверить [models.md](../pict/models.md), фактический `models.py` и состояние миграций через `python manage.py showmigrations pict`.
- После создания миграции добавить ее в эту таблицу и обновить итоговую схему при необходимости.
- При проблеме перехода между конкретными версиями открыть соответствующий исходный файл миграции: сводка не содержит всех сериализованных параметров полей.

## `0025_integrations`

Создаёт `Integration` для HTML/JS-вставок сайта и `IntegrationRevision` для истории снимков. Зависит от `0024_contactrequest_viewed_at` и заменяемой модели пользователя (`AUTH_USER_MODEL`). Новые интеграции по умолчанию выключены; миграция не создаёт записей и не изменяет существующие данные каталога. См. [интеграции](../pict/integrations.md).

## `0026_tag_search_terms`

Зависит от `0025_integrations` и выполняется в безопасной последовательности:

1. добавляет в `TagPict` временно nullable-поле `normalized_tag` без уникальности;
2. нормализует все существующие теги по тем же правилам NFKC, `casefold()`, `ё` → `е` и поисковых разделителей;
3. записывает значения через `bulk_update()` пачками до 500 строк;
4. делает поле обязательным и уникальным;
5. создает `TagAlias` с внешним ключом на основной тег, исходным `alias` и уникальным `normalized_alias`.

Data-миграция не пытается автоматически объединять данные. Она прерывается с `RuntimeError`, если нормализованное значение не содержит ни одной буквы или цифры, длиннее 200 символов или совпадает у двух существующих тегов. Перед повторным применением конфликтующие исходные теги нужно исправить вручную. Обратная функция для заполнения значений — `RunPython.noop`; откат схемы удаляет созданные поле и таблицу обычными обратными операциями миграции. Синонимы миграция не создает.

Уникальность каждого нормализованного поля обеспечивается внутри его таблицы. Запрет совпадения значения между `TagPict.normalized_tag` и `TagAlias.normalized_alias` реализован валидацией текущих моделей, а не межтабличным ограничением этой миграции. Правила поиска описаны в [search.md](../pict/search.md).

## `0027_taxonomy_seo_content`

Зависит от `0026_tag_search_terms` и добавляет одинаковые четыре поля непосредственно в таблицы `Category` и `TagPict`: `seo_h1` и `seo_title` длиной до 200 символов, `seo_description` длиной до 320 символов и неограниченный `intro_text`. Все поля допускают пустую строку, поэтому миграция не требует заполнения существующих записей и не меняет их публичное отображение: представления продолжают использовать стандартные значения до ручного заполнения полей в Django Admin.

Абстрактный Python-класс `SeoLandingContent` не создаёт отдельной таблицы и не добавляет связей между моделями.

## `sitecontent/0001_initial`

Создаёт единственную системную модель `SiteMenu` и связанные одноуровневые строки `MenuItem`. В той же миграции прежний Python-список переносится в пять включённых пунктов «Основного меню»:

1. «Главная страница» — `/`, порядок 10;
2. «Каталог скинали» — `/skinali/`, порядок 20;
3. «Наши работы» — `/foto-skinali-iz-stekla/`, порядок 30;
4. «Услуги дизайнера» — `/designer`, порядок 40;
5. «Связаться с нами» — `/about/`, порядок 50.

Миграция не зависит от `pict`: URL сохраняются как текстовые значения, а связь с моделями каталога не создаётся. Обратная data-миграция удаляет системное меню и каскадно его пункты. Текущая модель, admin и публичный вывод описаны в [sitecontent/overview.md](../sitecontent/overview.md).
